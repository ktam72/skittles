# Skittles — 設計ドキュメント

## 概要

Skittles は X68000 用ファイラー mint.x (Madoka INTerpreter) の設計思想を継承した、
モダンな TUI（ターミナルユーザインターフェース）ファイルマネージャである。

### プロジェクト目標

- 2画面ペインが暗黙の source/destination コンテキストを提供し、CLI操作を直感的にする
- macOS / Linux / Windows のクロスプラットフォーム対応
- Homebrew 一発インストールを可能とするシングルバイナリ配布
- 拡張子→アクションマッピングの外部設定分離

---

## アーキテクチャ

### レイヤー構造

```
main.go                  ← エントリポイント、tea.NewProgram
  └─ input_darwin.go     ← macOS: 起動時に英数入力切替（ビルドタグ分離）
  └─ input_other.go      ← Linux/Windows: 何もしない

ui/
  ├─ model.go            ← Elm Model/Update（全状態管理・イベントディスパッチ）
  ├─ pane.go             ← ファイルペイン（カーソル/マーク/ソート/表示）
  ├─ console.go          ← コンソールペイン（出力バッファ/コマンド履歴/入力）
  └─ view.go             ← lipgloss レンダリング、3ペインレイアウト

fs/
  ├─ entry.go            ← ファイルエントリ、ディレクトリ一覧、ソート
  └─ ops.go              ← Copy/Move/Delete/Touch

actions/
  └─ registry.go         ← マジックバイト/拡張子/ファイル名によるアクション解決

config.yaml              ← 拡張子→アクション定義（ユーザー編集可能）
```

### データフロー

```
キー入力 (tea.KeyMsg)
  → Update (model.go)
    → ペイン状態変更 / ファイル操作 / コマンド実行
  → View (view.go)
    → lipgloss でレンダリング
  → ターミナル出力
```

---

## 主要コンポーネント

### 1. 3ペインシステム

| ペイン | フォーカス値 | 役割 |
|--------|------------|------|
| Left   | `focusLeft` (0) | 左ウィンドウのディレクトリ一覧表示 |
| Right  | `focusRight` (1) | 右ウィンドウのディレクトリ一覧表示 |
| Console | `focusConsole` (2) | コマンド実行結果・コマンドライン入力 |

Tab キーで 3ペインを巡回。アクティブペインは青色ボーダー、非アクティブは灰色。

### 2. 暗黙の source/destination コンテキスト

フォーカス中のペイン = source、反対のファイルペイン = destination として動作する。

- `c` → フォーカスペインの選択エントリを反対ペインのカレントディレクトリへ**コピー**
- `m` → 同様に**移動**
- マークがある場合は全マークエントリ、ない場合はカーソル上の1エントリのみ

### 3. 拡張子→アクション解決 (actions/registry.go)

4段階の優先度でアクションを解決:

| 優先度 | 種別 | 例 |
|--------|------|-----|
| 0 | マジックバイト | `^GIF8` → ビューア |
| 1 | ファイル名完全一致 | `!Makefile` → ビューア |
| 2 | 拡張子 | `.go` → ビューア、`.zip` → unzip |
| 3 | デフォルト | 未登録 → `$EDITOR` |

### 4. コンソールペイン

- コマンド実行結果を出力バッファに蓄積
- 履歴対応（↑↓で呼び出し）
- ファイルペインで `!` 押下でコンソールに直接ジャンプ
- ESC でファイルペインに復帰

### 5. ビルトインビューア

- テキストファイルを ES で開かずにインライン表示
- ↑↓/PgUp/PgDown でスクロール
- ヘッダーに行数/位置表示
- ESC で終了

---

## キーバインド一覧

### ブラウズモード（ファイルペインフォーカス時）

| キー | 機能 |
|------|------|
| ↑ / k | カーソル上 |
| ↓ / j | カーソル下 |
| PgUp / b | 1ページ上 |
| PgDown / Space | 1ページ下 |
| Home / g | 先頭へ |
| End / G | 末尾へ |
| → / l | ディレクトリ進入 / ファイルをアクション実行 |
| Enter | ディレクトリ進入 / ファイルをアクション実行 |
| ← / h | 親ディレクトリへ |
| Tab | ペインフォーカス切替（Left→Right→Console→Left） |
| Backspace | マーク / マーク解除 |
| a | 全ファイルマーク |
| c | 選択エントリを反対ペインへコピー |
| m | 選択エントリを反対ペインへ移動 |
| d | 選択エントリを削除 |
| r | カレントディレクトリ再読込 |
| sr | ソート順切替（名前→日時→拡張子→サイズ→ループ） |
| E | エディタで開く |
| ! | コンソールペインへジャンプ |
| ESC | 終了確認 |

### コンソールモード

| キー | 機能 |
|------|------|
| 文字入力 | コマンドライン編集 |
| Enter | コマンド実行 |
| ↑ | 履歴戻る（入力なし時は出力スクロール） |
| ↓ | 履歴進む |
| Backspace | 1文字削除 |
| ESC | ファイルペインに復帰 |

### ビューモード

| キー | 機能 |
|------|------|
| ↑ | 1行上スクロール |
| ↓ | 1行下スクロール |
| PgUp | 1ページ上 |
| PgDown | 1ページ下 |
| ESC | ビューア終了、ファイルペイン復帰 |

---

## mint.x からの差分・省略点

### 実装済み（mint.x 相当）

- 2画面ファイルブラウズ
- ファイル操作（cp/mv/rm/mkdir/touch）
- 拡張子→アクション解決
- ビルトインテキストビューア
- コマンドライン実行と出力表示
- マーク機能
- ソート切替

### 未実装（今後の拡張候補）

| 機能 | mint.x の該当モジュール | 備考 |
|------|----------------------|------|
| Madoka スクリプト | madoka3.s (6,892行) | 独自言語、gopher-lua で代替可能 |
| グラフィックビューア | gvon.s (4,864行) | QuickLook 連携で代替 |
| アーカイバ | mintarc.s (4,182行) | libarchive バインディング |
| 音楽プレイヤー | music.s (1,045行) | スコープ外 |
| プラグインシステム | plug-in/ | Protocol ベースで再設計可能 |
| インクリメンタルサーチ | isearch.s (879行) | 実装容易 |
| ファイル名パターンマッチ | patternmatch.s (1,460行) | Go 標準の filepath.Match 相当 |
| 画面モード切替（%winf） | winf_*.s | 表示フォーマットのカスタマイズ |
| ファイル比較 | filecmp.s | diff 統合 |
| バッチリネーム | rename.s | 実装容易 |

---

## ビルド・配布

### ローカルビルド

```bash
go build -o skittles .
```

### クロスコンパイル

```bash
# Linux
GOOS=linux GOARCH=amd64 go build -o skittles-linux .
# Windows
GOOS=windows GOARCH=amd64 go build -o skittles.exe .
```

### Homebrew Formula（将来）

```ruby
class Skittles < Formula
  desc "Two-pane TUI file manager inspired by X68000 mint.x"
  homepage "https://github.com/ktam/skittles"
  # ...
  def install
    system "go", "build", "-o", bin/"skittles"
  end
end
```

### Go インストール（go install）

```bash
go install github.com/ktam/skittles@latest
```

---

## プラットフォーム対応状況

| OS | ビルド | 動作 | 備考 |
|----|--------|------|------|
| macOS | ✅ | ✅ | 入力ソース切替は osascript（アクセシビリティ許可が必要） |
| Linux | ✅ | ✅（検証中） | 入力ソース切替は no-op |
| Windows | ✅ | ✅（検証中） | `go install` 対応、コンソール制御は bubbletea 内蔵 |

注意: 3ペインのレイアウト計算は全プラットフォームで同一のロジックを使用。異なるのは `input_darwin.go` / `input_other.go` のビルドタグ分離のみ。

---

## 技術スタック選定理由

- **Go**: シングルバイナリ、標準ライブラリだけでファイル I/O/プロセス実行が完結、クロスコンパイルの容易さ
- **bubbletea**: Elm アーキテクチャによる状態管理の明瞭さ、3ペインのフォーカス管理に適合
- **lipgloss**: スタイルの関数型合成、条件分岐（アクティブ/非アクティブ）の表現力
- **設定は YAML**: 人間が読み書きしやすく、コメント可能、標準ライブラリでパース可能

---

## 参考: mint.x ソースコード分析

Skittles の設計は以下のリバースエンジニアリング結果に基づく。

| 計測項目 | 値 |
|----------|-----|
| mint.x 総コード行数 | ~65,000行（68k アセンブラ 24ファイル + プラグイン 9ファイル） |
| アーキテクチャ | Motorola 68000 / Human68k v3 |
| 最大モジュール | mint.s (14,367行) - メインループ/ディスパッチ |
| 最大機能モジュール | look.s (7,881行) - ファイルビュアー |
| ライセンス | GPL v2（コード参考のための解析のみ、直接流用せず） |

mint.x の独自価値は「2画面による暗黙コンテキスト」にあり、ハードウェア依存部（IOCS/CRTC/VRAM 直アクセス〜800行相当）と OS 依存部（Human68k DOS コール〜200ヶ所）は全て切り捨てている。

---

## 開発ロードマップ（任意）

```
Phase 1 ─ 基本2画面 + ファイル操作       ✅ 完了
Phase 2 ─ コンソールペイン + マーク       ✅ 完了
Phase 3 ─ リファクタリング・設定外部化     🔄 現在
Phase 4 ─ インクリメンタルサーチ          ⬜ 未着手
Phase 5 ─ クロスプラットフォーム検証      ⬜ 未着手
Phase 6 ─ Homebrew Formula / 配布        ⬜ 未着手
Phase 7 ─ プラグインシステム              ⬜ 未着手
```
